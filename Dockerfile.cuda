FROM nvidia/cuda:12.1.1-cudnn8-runtime-ubuntu22.04
RUN apt-get update && apt-get install -y --no-install-recommends python3.11 python3-pip git && rm -rf /var/lib/apt/lists/*
WORKDIR /work
COPY . .
RUN python3.11 -m pip install --no-cache-dir '.[ml]'
ENTRYPOINT ["precision-md"]
